{% extends '@Admin/adminLayout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock stylesheets %}

{% block page_content -%}
    <h3>{{ 'Currency'|trans }}</h3>

    <table class="record_properties table table-bordered table-striped">
        <tbody>
        <tr>
            <th>{{ 'Name'|trans }}</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>{{ 'Code'|trans }}</th>
            <td>{{ entity.code }}</td>
        </tr>
        <tr>
            <th>{{ 'Exchange Rate'|trans }}</th>
            <td>{{ entity.exchangeRate }}</td>
        </tr>
        <tr>
            <th>{{ 'Enabled'|trans }}</th>
            <td>{{ entity.enabled }}</td>
        </tr>
        </tbody>
    </table>

    <ul class="list-group">
        <li class="list-group-item">
            <a href="{{ path('admin_currency') }}">
                {{ 'Back to the list'|trans }}
            </a>
        </li>
        <li class="list-group-item">
            <a href="{{ path('admin_currency_edit', { 'id': entity.id }) }}">
                {{ 'Edit'|trans }}
            </a>
        </li>
        <li class="list-group-item">
            {{ form_start(delete_form, { 'attr': {'class': 'bootbox-submit-confirm'} }) }}
            {{ form_widget(delete_form) }}
                <input type="submit" value="{{ 'Delete'|trans }}" class="btn btn-danger"/>
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock page_content %}

{% block javascripts %}
    {{ parent() }}
{% endblock javascripts %}
